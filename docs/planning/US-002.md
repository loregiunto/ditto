# US-002: Pubblicazione e disattivazione listing — Piano di Implementazione

**Generato da:** Archetipo Planning Team
**Data:** 2026-05-06

---

## User Story

**Epic:** EP-001 — Listing Management
**Priorità:** HIGH | **Story Points:** 2

**Story**
Come Giulia, voglio pubblicare e disattivare il mio listing, così da controllare quando il mio bagno è visibile e prenotabile sulla piattaforma.

**Criteri di Accettazione**
- [ ] L'host può cambiare lo status del listing tra `active` e `inactive` dalla dashboard host
- [ ] Solo i listing `active` appaiono sulla mappa e nei risultati di ricerca
- [ ] Il cambio di status ha effetto immediato (entro 1 minuto)
- [ ] Un listing non può essere pubblicato senza aver completato Stripe Connect (EP-004) e la verifica identità (EP-004)

---

## Soluzione Tecnica

L'approccio estende il modello `Listing` introdotto da US-001 con i nuovi valori di status `ACTIVE` e `INACTIVE`, e aggiunge un singolo endpoint `PATCH` che governa tutte le transizioni di stato (prima pubblicazione `DRAFT → ACTIVE`, disattivazione `ACTIVE → INACTIVE`, riattivazione `INACTIVE → ACTIVE`); la pre-condizione di pubblicazione (Stripe Connect + identità verificata) appartiene a US-013/US-014 e qui viene scaffoldata come *gate* server-side che legge due campi opzionali su `User`, lasciando pronto il contratto di sicurezza senza anticipare l'integrazione Stripe. La UI è una pagina minimale `/host/dashboard` con la lista dei listing dell'host e un toggle: la versione completa della dashboard (KPI, prenotazioni, recensioni) è scope di US-019.

- **Schema Prisma:** estendere l'enum `ListingStatus` con `ACTIVE` e `INACTIVE` (oltre a `DRAFT` di US-001); aggiungere su `User` i campi nullable `stripeAccountId String?` e `identityStatus String?` (defaultati a `null`) che US-013/US-014 popoleranno
- **Helper di gate:** nuovo `src/lib/listings/can-publish.ts` con funzione pura `canPublish(user)` che ritorna `{ ok: true } | { ok: false, reason: 'STRIPE_NOT_CONNECTED' | 'IDENTITY_NOT_VERIFIED' }`; usato sia dall'API sia dalla UI per messaggi coerenti
- **API:** `PATCH /api/listings/[id]/status` autenticato via `getCurrentUser()`, body validato con zod `{ status: 'active' | 'inactive' }`, controlla ownership (`listing.hostId === user.id` → altrimenti 403/404), applica il gate `canPublish` solo nella transizione verso `ACTIVE`, esegue `prisma.listing.update`, infine `revalidatePath('/host/dashboard')` e `revalidatePath('/')` per garantire l'effetto entro 1 minuto sui consumer Next.js cached
- **Pagina `/host/dashboard`:** server component che recupera i listing dell'host (`prisma.listing.findMany({ where: { hostId } })`), li elenca in card con titolo, status badge e azioni; client component `<ListingStatusToggle>` con bottone contestuale ("Pubblica" se `DRAFT`/`INACTIVE`, "Disattiva" se `ACTIVE`) che chiama l'API, mostra toast in caso di successo e visualizza il motivo di blocco se il gate fallisce
- **Coerenza visiva:** card e badge ispirati ai pattern già presenti in `docs/Mockup/HomeRest/host-dashboard.jsx` — versione semplificata, US-019 espande la dashboard
- **Discovery contract:** le future query pubbliche (mappa US-005, ricerca US-006) dovranno filtrare `where: { status: 'ACTIVE' }`; oggi nessuna route di consumo esiste, quindi nulla da modificare in questa storia
- **Middleware:** la protezione di `/host/*` è già pianificata in US-001 (TASK-11); questa storia non introduce duplicazione

---

## Strategia di Test

Test piramidali con focus sull'API (cuore della storia) e un singolo e2e Playwright sul flusso di toggle dalla dashboard.

- **Unit** — `canPublish(user)`: ritorna `ok` solo se entrambi `stripeAccountId` e `identityStatus === 'verified'` sono presenti; ogni assenza produce la `reason` attesa
- **Unit** — schema zod del body `PATCH`: rifiuta valori diversi da `active`/`inactive`, body vuoti, payload con campi extra
- **Integration** — `PATCH /api/listings/[id]/status`: happy path `DRAFT → ACTIVE` con utente che soddisfa il gate; happy path `ACTIVE ↔ INACTIVE`; 401 senza sessione; 403/404 su listing di altro host; 422 con `reason` corretto se manca Stripe o identità; verificare che `revalidatePath` sia invocato (spy)
- **Integration** — verifica DB: dopo PATCH lo status è persistito e `updatedAt` aggiornato; nessun side-effect su altre colonne del listing
- **E2E** — Playwright: signin host → seed di un listing `DRAFT` con `stripeAccountId` e `identityStatus='verified'` → naviga a `/host/dashboard` → click "Pubblica" → badge diventa "Attivo"; secondo run senza i campi gate → click "Pubblica" → toast con motivo di blocco e status invariato

---

## Task di Implementazione

| Stato | # | Task | Descrizione | Tipo | Dipendenze |
|---|---|---|---|---|---|
| DONE | TASK-01 | Estendere enum `ListingStatus` + campi gate su User | Aggiungere `ACTIVE`, `INACTIVE` all'enum; aggiungere `stripeAccountId String?` e `identityStatus String?` su `User`; eseguire `prisma db push`. | Impl | - |
| DONE | TASK-02 | Helper `canPublish` | Creare `src/lib/listings/can-publish.ts` con la funzione pura e i tipi delle reason. | Impl | TASK-01 |
| DONE | TASK-03 | Test unit `canPublish` | Coprire i tre rami: ok, mancanza Stripe, mancanza identità verificata. | Test | TASK-02 |
| DONE | TASK-04 | API route `PATCH /api/listings/[id]/status` | Implementare `src/app/api/listings/[id]/status/route.ts`: auth, zod, ownership, gate sulla transizione verso ACTIVE, update, `revalidatePath`. | Impl | TASK-02 |
| DONE | TASK-05 | Test integration API status | Coprire happy path DRAFT→ACTIVE, ACTIVE↔INACTIVE, 401, 403/404, 422 con reason. | Test | TASK-04 |
| DONE | TASK-06 | Pagina `/host/dashboard` (lista listing) | Server component che recupera e mostra i listing dell'host con badge status; layout coerente con il mockup di riferimento. | Impl | TASK-01 |
| DONE | TASK-07 | Componente client `<ListingStatusToggle>` | Bottone contestuale che invoca l'API, gestisce loading/toast, mostra il motivo di blocco se il gate fallisce. | Impl | TASK-04, TASK-06 |
| DONE | TASK-08 | E2E Playwright toggle | Flusso happy path pubblica/disattiva + scenario gate non soddisfatto con messaggio di blocco. | Test | TASK-07 |

---

> 🎨 I mockup di riferimento sono in `docs/Mockup/HomeRest/host-dashboard.jsx` (versione completa — questa storia ne implementa solo la sezione lista listing).

_Piano generato via Archetipo Planning — 2026-05-06_
