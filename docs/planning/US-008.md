# US-008: Pagina di dettaglio listing â€” Piano di Implementazione

**Generato da:** Archetipo Planning Team
**Data:** 2026-05-06

---

## User Story

**Epic:** EP-002 â€” Discovery & Mappa
**PrioritÃ :** HIGH | **Story Points:** 3

**Story**
Come Marco, voglio vedere tutti i dettagli di un bagno (foto, descrizione, disponibilitÃ , host, recensioni) in una pagina dedicata, cosÃ¬ da prendere una decisione informata prima di prenotare.

**Criteri di Accettazione**
- [ ] Gallery foto scorribile; descrizione completa; prezzo orario
- [ ] Badge tipo host (Privato ðŸ  / AttivitÃ  Commerciale ðŸª / Verificato âœ… / Super Host â­) visibili
- [ ] Indirizzo mostrato come quartiere/zona (non esatto) pre-prenotazione
- [ ] Fasce orarie disponibili mostrate in un selettore; slot giÃ  prenotati non selezionabili
- [ ] Valutazione media, numero di recensioni e lista delle ultime 5 recensioni
- [ ] CTA "Prenota ora" visibile e attiva; modalitÃ  di conferma (Istantanea / Su richiesta) indicata
- [ ] Se l'utente non Ã¨ autenticato, il CTA porta al login

---

## Soluzione Tecnica

L'approccio implementa una **route server pubblica `/listings/[id]`** che carica con Prisma `select` esplicito tutti i campi pubblici del listing, le `availabilityRules` e i dati host (nome, immagine, `identityStatus`), proietta gli slot prenotabili dei prossimi 7 giorni con `weeklyRulesToSlots` (riuso US-003 con `bookings = []` finchÃ© US-009 non introduce il modello `Booking`), e monta un client component `<ListingDetail>` con gallery scorribile, slot picker raggruppato per giorno e CTA. La privacy Ã¨ strutturale come in US-005: `addressFull` non viene mai letto nÃ© serializzato. I placeholder per le feature non ancora implementate (recensioni â€” US-015, badge Verificato/Super Host â€” US-014/US-017) sono incapsulati in un singolo modulo `src/lib/listings/detail.ts` con feature-flag espliciti, cosÃ¬ quando arriveranno le storie a valle basterÃ  rimuovere il flag senza riaprire il componente.

- **Schema Prisma:** aggiunta del campo `bookingMode BookingMode @default(INSTANT)` sull'`Listing` (nuovo enum `INSTANT | REQUEST`). Default retro-compatibile sui listing esistenti, nessuna migrazione manuale; il form di US-001 non viene toccato â€” l'estensione del form alla scelta della modalitÃ  Ã¨ tema di US-009/US-010. Da subito mostriamo il badge "Conferma istantanea" / "Su richiesta" sulla pagina detail.
- **Route `/listings/[id]/page.tsx` (server component, `export const dynamic = "force-dynamic"`):** Prisma `findUnique` con `select` esplicito che include `host: { name, image, identityStatus }`, `photos`, `availabilityRules` ed esclude `addressFull`. Se non trovato o `status !== 'ACTIVE'` â†’ `notFound()`. Recupera anche `getCurrentUser()` per decidere lo stato del CTA.
- **DTO + helper (`src/lib/listings/detail.ts`):** `toPublicDetailDTO(listing)` produce un `PublicListingDetail` strutturalmente privo di `addressFull` con foto ordinate per `order`, `verifiedBadge = identityStatus === 'verified'`, `superHostBadge = false` (costante esposta come `SUPER_HOST_PLACEHOLDER` finchÃ© US-017 non implementa il calcolo), e `bookingMode` propagato dallo schema. Test unit verifica strutturalmente l'assenza di `addressFull` tra le chiavi.
- **Slot picker:** il server proietta `weeklyRulesToSlots(rules, now, now + 7d, 30, [])`, raggruppa per giorno e passa al client. UI: tab orizzontale (oggi â†’ +6gg) + griglia di pill cliccabili da 30 minuti. Lo stato selezionato vive solo lato client (no submit â€” la prenotazione vera arriverÃ  con US-009). Il parametro `bookings` del helper Ã¨ giÃ  pronto a ricevere i booking confermati quando esisteranno.
- **Componente `<ListingDetail>` (client):** gallery scorribile via CSS `scroll-snap` (zero dipendenze nuove), descrizione, prezzo `â‚¬{cents/100}/h`, riga badge (`hostType` sempre, `Verificato âœ…` condizionale, `Super Host â­` condizionale, `Conferma istantanea` / `Su richiesta`), info host (avatar + nome), sezione recensioni. Stile coerente con `<ListingPreviewCard>` (palette `hr-*` giÃ  in `globals.css`).
- **Sezione recensioni:** componente `<ReviewsList>` con shape pronta (`{ averageRating, totalCount, items: ReviewItem[] }`) ma alimentato con `EMPTY_REVIEWS` placeholder fino a US-015. Stato vuoto: "Ancora nessuna recensione".
- **CTA "Prenota ora":**
  - Utente non autenticato â†’ `<Link href="/auth/signin?redirect=/listings/{id}">Accedi per prenotare</Link>`. Il callback OAuth esistente giÃ  gestisce il parametro `redirect`; in caso contrario va esteso (verifica in TASK-08).
  - Utente autenticato â†’ bottone disabilitato con tooltip "Disponibile a breve" (US-009 non ancora implementata). Lo slot resta selezionabile per dare feedback visivo. Quando arriverÃ  US-009 basterÃ  sostituire l'handler.
- **Wiring discovery:** in `src/components/discovery/listing-preview-card.tsx` il bottone "Dettagli" oggi disabilitato diventa `<Link href="/listings/{id}">`.
- **Privacy strutturale:** test integration verifica esplicitamente che il markup HTML della pagina non contenga mai la stringa di `addressFull` per nessun fixture. La query Prisma `select` impedisce il leak anche per regressione futura.
- **404 / privacy:** listing `DRAFT`/`INACTIVE` e id inesistenti restituiscono 404 standard via `notFound()` per evitare URL cacheati che continuino a girare.
- **Bundle hygiene:** zero nuove dipendenze. Il client component Ã¨ leggero e tree-shakable. La pagina Ã¨ dynamic-rendered come `/api/listings/map`.

---

## Strategia di Test

Test pyramid coerente con US-003/US-005: unit per la pura logica di shape della response e proiezione slot, integration sulla route renderizzata con DB reale (assenza `addressFull` nel HTML, 404 fuori da `ACTIVE`), un e2e Playwright sull'happy path autenticato e uno sul flusso non-auth â†’ login.

- **Unit** â€” `toPublicDetailDTO`: assenza di `addressFull` tra le chiavi, `verifiedBadge = true` sse `identityStatus === 'verified'`, `superHostBadge` sempre `false` (placeholder), photo ordinate per `order` ascendente, `bookingMode` propagato
- **Unit** â€” proiezione slot detail (riuso `weeklyRulesToSlots` su finestra 7 giorni): listing senza regole â†’ array vuoto, raggruppamento per giorno corretto, slot di oggi precedenti a `now` esclusi
- **Integration** â€” pagina `/listings/[id]`: render OK su `ACTIVE`; `notFound()` su `DRAFT`/`INACTIVE`/id inesistente; HTML renderizzato non contiene mai `addressFull` per alcun fixture; il CTA varia tra "Accedi per prenotare" (no session) e "Prenota ora" disabilitato (autenticato)
- **Integration** â€” verifica che il nuovo campo `bookingMode` sia serializzato correttamente (default `INSTANT` per i listing esistenti seedati prima della migration)
- **E2E Playwright** â€” flusso non auth: home â†’ click pin â†’ preview â†’ "Dettagli" â†’ pagina detail â†’ CTA "Accedi per prenotare" â†’ atterra su `/auth/signin?redirect=/listings/{id}`
- **E2E Playwright** â€” flusso auth: signin â†’ apri detail di listing seed â†’ gallery scorribile via swipe orizzontale, badge `Privato` + `Conferma istantanea` visibili, slot picker mostra almeno un giorno con pill, click su pill la marca selezionata, CTA disabilitato con tooltip "Disponibile a breve"

---

## Task di Implementazione

| Stato | # | Task | Descrizione | Tipo | Dipendenze |
|---|---|---|---|---|---|
| DONE | TASK-01 | Schema Prisma `bookingMode` | Aggiungere enum `BookingMode { INSTANT, REQUEST }` e campo `bookingMode @default(INSTANT)` su `Listing`; eseguire `prisma db push`. | Impl | - |
| DONE | TASK-02 | DTO + helper `detail.ts` | Creare `src/lib/listings/detail.ts` con tipo `PublicListingDetail`, `toPublicDetailDTO(listing)` (no `addressFull`, `verifiedBadge`, `superHostBadge` placeholder, foto ordinate, `bookingMode`) e costanti `DETAIL_HORIZON_DAYS = 7`, `EMPTY_REVIEWS`. | Impl | TASK-01 |
| DONE | TASK-03 | Test unit DTO detail + slot horizon | Coprire: assenza `addressFull`, mapping `verifiedBadge` su `identityStatus`, `superHostBadge` placeholder, ordering foto, proiezione slot 7 giorni con regole tipiche e raggruppamento per giorno. | Test | TASK-02 |
| DONE | TASK-04 | Pagina `/listings/[id]/page.tsx` (server) | Server component `dynamic = "force-dynamic"`: Prisma `findUnique` con `select` esplicito (no `addressFull`), `notFound()` se status â‰  ACTIVE, proietta slot `weeklyRulesToSlots(..., now, now+7d, 30, [])`, passa al client component insieme allo stato auth. | Impl | TASK-02 |
| DONE | TASK-05 | Componente `<ListingDetail>` (client) | Gallery scroll-snap, descrizione, prezzo, riga badge (hostType + Verificato/Super Host condizionali + bookingMode), info host, sezione recensioni placeholder. Stile palette `hr-*` coerente con preview card. | Impl | TASK-04 |
| DONE | TASK-06 | Slot picker giorno-pill | Tab orizzontale (oggi â†’ +6gg) + griglia pill 30min cliccabili; stato selezione client; empty-state per giorno senza slot. | Impl | TASK-05 |
| DONE | TASK-07 | Sezione `<ReviewsList>` placeholder | Componente con shape pronta per US-015 + stato vuoto "Ancora nessuna recensione" + media/conteggio nulli. | Impl | TASK-05 |
| DONE | TASK-08 | CTA "Prenota ora" + redirect login | Link a `/auth/signin?redirect=/listings/{id}` per non-auth; bottone disabilitato + tooltip per auth. Verificare che il callback OAuth (`src/app/auth/callback/route.ts`) supporti il param `redirect`; se non lo fa, estenderlo. | Impl | TASK-04, TASK-05 |
| DONE | TASK-09 | Wire "Dettagli" da preview card | In `src/components/discovery/listing-preview-card.tsx` sostituire il bottone disabilitato con `<Link href="/listings/{id}">Dettagli</Link>`. | Impl | TASK-04 |
| DONE | TASK-10 | Test integration pagina detail | Render OK su `ACTIVE`, `notFound()` su `DRAFT`/`INACTIVE`/inesistente, HTML scan privo di `addressFull`, CTA varia per stato auth, `bookingMode` di default presente nel markup. | Test | TASK-04, TASK-05, TASK-08 |
| DONE | TASK-11 | E2E Playwright flusso non-auth | Home â†’ pin â†’ preview â†’ "Dettagli" â†’ "Accedi per prenotare" â†’ `/auth/signin?redirect=...`. | Test | TASK-08, TASK-09 |
| DONE | TASK-12 | E2E Playwright flusso auth | Signin â†’ detail di listing seed â†’ gallery scorribile, badge tipo host + bookingMode visibili, click su pill slot la seleziona, CTA disabilitato con tooltip. | Test | TASK-06, TASK-07, TASK-08 |

---

> ðŸŽ¨ I mockup per questa storia sono disponibili in `docs/mockups/US-008/`

_Piano generato via Archetipo Planning â€” 2026-05-06_

